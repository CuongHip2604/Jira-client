import styled from 'styled-components';

import { sizes } from 'shared/utils/styles';

const paddingLeft = sizes.appNavBarLeftWidth + 32;

export const ProjectContainer = styled.div`
  padding: 25px 32px 50px ${paddingLeft}px;
`
